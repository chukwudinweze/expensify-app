import React from "react";
import { Link } from "react-router-dom";
import ExpenseList from './ExpenseList'

const Home = () => (
  <div>
    <ExpenseList/>
  </div>
);

export default Home;
